add_executable(ppp_statistics ppp_statistics.cpp)
target_compile_features(ppp_statistics PRIVATE cxx_std_17)
if(MSVC)
    target_compile_options(ppp_statistics PRIVATE /W4 /utf-8)
    target_compile_definitions(ppp_statistics PRIVATE _CRT_SECURE_NO_WARNINGS)
else()
    target_compile_options(ppp_statistics PRIVATE -Wall -Wextra -Wpedantic)
endif()
if(BUILD_TESTING)
    add_test(NAME ppp_statistics COMMAND ppp_statistics --self-test)
endif()

add_executable(inspect_rtcm_input inspect_rtcm_input.c)
target_link_libraries(inspect_rtcm_input PRIVATE B2bLib)
